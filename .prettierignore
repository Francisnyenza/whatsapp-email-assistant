node_modules
dist
.next
coverage
generated
pnpm-lock.yaml
*.md.tpl

# Generated by demo/build.sh. Gitignored, so CI never sees them — but they exist
# on a machine that has run the demo, and a `format:check` that passes in CI and
# fails locally is a check people learn to ignore.
demo/bundle.js
demo/page.html
